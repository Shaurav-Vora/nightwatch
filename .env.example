# Copy to .env and fill in. .env must never be committed.
FORTYGUARD_API_KEY=your_key_here

# Free, instant: https://api.census.gov/data/key_signup.html
# Only needed for population.py — nothing to do with FortyGuard credits.
CENSUS_API_KEY=
