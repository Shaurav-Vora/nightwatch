requests>=2.31
python-dotenv>=1.0
pytest>=8.0

# Windows and some slim Linux images ship no system timezone database, so
# zoneinfo has nothing to read. Without this, every UTC->local conversion
# raises ZoneInfoNotFoundError. Harmless no-op where a system tzdb exists.
tzdata>=2024.1
